from mystery_word import mystery_game_process


def successful_replay_game():
    assert myster_game_process(playing_again=True) == True

def successful_replay_game():
    assert myster_game_process(playing_again=False) == True
